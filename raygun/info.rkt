#lang info

(define collection 'multi)

(define deps '("raygun-lib"
               "raygun-doc"))
(define implies '("raygun-lib"
                  "raygun-doc"))

(define pkg-desc "Approaching Something Closer To A 'Game-Engine' In Raykit.")

(define pkg-authors '(megalisp))

(define license
  '(Apache-2.0 OR MIT))
